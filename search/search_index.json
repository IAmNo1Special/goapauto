{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"GOAP Planner Documentation","text":"<p>Welcome to the documentation for goapauto, a strictly-typed Goal-Oriented Action Planning system for Python.</p>"},{"location":"#user-guide","title":"\ud83d\udcda User Guide","text":"<p>Start here to learn how to build agents.</p> <ul> <li>Getting Started: Build your first agent in 5 minutes.</li> <li>Core Concepts: Understanding WorldState, Goals, and the Planning Loop.</li> </ul>"},{"location":"#api-reference","title":"\u2699\ufe0f API Reference","text":"<p>Detailed reference for classes and functions.</p> <ul> <li>WorldState: State management.</li> <li>Goals: Defining objectives.</li> <li>Actions: Defining behaviors and effects.</li> <li>Planner: The A* planning engine.</li> <li>Sensors &amp; Arbitrators: Advanced sensing and goal selection.</li> <li>Visualizer: Debugging tools.</li> </ul>"},{"location":"reference/actions/","title":"Actions API Reference","text":"<p>The <code>Action</code> class represents an atomic action the agent can take.</p> <pre><code>from goapauto.models.actions import Action, Increment\n\naction = Action(\n    name=\"chop_wood\",\n    preconditions={\"has_axe\": True},\n    effects={\"wood\": Increment(1)},\n    cost=1.0,           # int or float (default: 1)\n    duration=2.0,       # optional duration for temporal planning\n    description=\"Chops wood using an available axe\" # optional description\n)\n</code></pre>"},{"location":"reference/actions/#multi-dimensional-costs","title":"Multi-dimensional Costs","text":"<p>Action costs can be scalar (float) or multi-dimensional (dict/list) for complex optimization:</p> <pre><code># Dictionary costs with named dimensions\naction = Action(\n    name=\"navigate\",\n    preconditions={},\n    effects={\"location\": Set(\"target\")},\n    cost={\"time\": 30.0, \"energy\": 5.0, \"risk\": 2.0}\n)\n\n# List costs with positional dimensions\naction = Action(\n    name=\"build\",\n    preconditions={},\n    effects={\"structure\": True},\n    cost=[10.0, 5.0, 2.0]  # [time, energy, cost]\n)\n</code></pre> <p>Use with <code>Planner(cost_weights=...)</code> to optimize weighted combinations.</p>"},{"location":"reference/actions/#duration-description","title":"Duration &amp; Description","text":"<pre><code>action = Action(\n    name=\"build\",\n    preconditions={},\n    effects={\"structure\": True},\n    cost=10.0,\n    duration=30.0,  # seconds\n    description=\"Constructs a shelter structure\"\n)\n</code></pre>"},{"location":"reference/actions/#goapauto.models.actions.Action","title":"<code>goapauto.models.actions.Action</code>  <code>dataclass</code>","text":"<p>Represents an action the agent can take.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Unique identifier for the action</p> <code>preconditions</code> <code>dict[str, Any | Predicate | Callable[[Any], bool]]</code> <p>Dictionary of state requirements that must be met for the action to be applicable</p> <code>effects</code> <code>dict[str, Any | Effect | Callable[[Any], Any]]</code> <p>Dictionary of state changes that result from applying this action</p> <code>cost</code> <code>int | float | dict[str, float] | list[float]</code> <p>The cost of executing this action (used for pathfinding). Can be a single float   or a dict/list for multi-dimensional costs (e.g., {\"time\": 30.0, \"energy\": 5.0}).</p> <code>duration</code> <code>float | None</code> <p>Optional duration in seconds (for temporal planning)</p> <code>description</code> <code>str | None</code> <p>Optional human-readable description of what the action does</p>"},{"location":"reference/actions/#goapauto.models.actions.Action.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Validate the action after initialization.</p>"},{"location":"reference/actions/#goapauto.models.actions.Action.__repr__","title":"<code>__repr__()</code>","text":"<p>Return the canonical string representation of the action.</p>"},{"location":"reference/actions/#goapauto.models.actions.Action.__str__","title":"<code>__str__()</code>","text":"<p>Return a string representation of the action.</p>"},{"location":"reference/actions/#goapauto.models.actions.Action.apply","title":"<code>apply(state)</code>","text":"<p>Apply this action to the current state and return a new state.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>Any</code> <p>The current world state to apply the action to</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A new state with the action's effects applied</p>"},{"location":"reference/actions/#goapauto.models.actions.Action.async_apply","title":"<code>async_apply(state)</code>  <code>async</code>","text":"<p>Asynchronously apply this action to the current state and return a new state.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>Any</code> <p>The current world state to apply the action to</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A new state with the action's effects applied</p>"},{"location":"reference/actions/#goapauto.models.actions.Action.is_applicable","title":"<code>is_applicable(state)</code>","text":"<p>Check if this action can be applied to the given state.</p> <p>A state missing a precondition key is not applicable. Errors raised by precondition predicates propagate to the caller -- a broken predicate fails loudly instead of silently disabling the action.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>Any</code> <p>The current world state to check against</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all preconditions are met, False otherwise</p>"},{"location":"reference/actions/#action-collections","title":"Action Collections","text":""},{"location":"reference/actions/#goapauto.models.actions.Actions","title":"<code>goapauto.models.actions.Actions</code>","text":"<p>Manages a collection of available actions for the GOAP planner.</p> <p>This class provides methods to add, retrieve, and manage actions that can be used by the planner to achieve goals. It ensures that all actions are valid and provides efficient lookup and iteration capabilities.</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.__contains__","title":"<code>__contains__(name)</code>","text":"<p>Check if an action with the given name exists.</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.__init__","title":"<code>__init__()</code>","text":"<p>Initialize an empty collection of actions.</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.__iter__","title":"<code>__iter__()</code>","text":"<p>Return an iterator over all actions.</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.__len__","title":"<code>__len__()</code>","text":"<p>Return the number of actions in the collection.</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.__repr__","title":"<code>__repr__()</code>","text":"<p>Return a detailed string representation of the actions collection.</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.__str__","title":"<code>__str__()</code>","text":"<p>Return a string representation of the actions collection.</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.add_action","title":"<code>add_action(name, preconditions, effects, cost=1, duration=None, description=None)</code>","text":"<p>Add a single action to the collection.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Unique identifier for the action</p> required <code>preconditions</code> <code>dict[str, Any]</code> <p>Dictionary of state requirements for the action</p> required <code>effects</code> <code>dict[str, Any]</code> <p>Dictionary of state changes caused by the action</p> required <code>cost</code> <code>int | float | dict[str, float] | list[float]</code> <p>The cost of executing this action (default: 1)</p> <code>1</code> <code>duration</code> <code>float | None</code> <p>Optional duration in seconds</p> <code>None</code> <code>description</code> <code>str | None</code> <p>Optional human-readable description</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an action with the same name already exists</p> <code>TypeError</code> <p>If any parameter has an invalid type</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.add_actions","title":"<code>add_actions(action_definitions)</code>","text":"<p>Add multiple actions to the collection.</p> <p>Parameters:</p> Name Type Description Default <code>action_definitions</code> <code>Sequence[tuple[Any, ...] | Action]</code> <p>List of action definitions where each definition is an Action object or a tuple in format: - (name, preconditions, effects, cost) - (name, preconditions, effects, cost, duration) - (name, preconditions, effects, cost, description) - (name, preconditions, effects, cost, duration, description)</p> required Example <p>actions = Actions() actions.add_actions([     (\"open_door\", {\"door_locked\": False}, {\"door_open\": True}, 1),     (\"unlock_door\", {\"has_key\": True}, {\"door_locked\": False}, 2, \"Unlocks door\") ])</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.clear_actions","title":"<code>clear_actions()</code>","text":"<p>Remove all actions from the collection.</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.filter_actions","title":"<code>filter_actions(state)</code>","text":"<p>Get a list of all actions that can be applied to the given state.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>Any</code> <p>The state to check against action preconditions</p> required <p>Returns:</p> Type Description <code>list[Action]</code> <p>A list of applicable Action objects</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.get_action","title":"<code>get_action(name)</code>","text":"<p>Retrieve an action by its name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the action to retrieve</p> required <p>Returns:</p> Type Description <code>Action | None</code> <p>The Action object if found, None otherwise</p>"},{"location":"reference/actions/#goapauto.models.actions.Actions.get_actions","title":"<code>get_actions()</code>","text":"<p>Get a list of all actions in the collection.</p> <p>Returns:</p> Type Description <code>list[Action]</code> <p>A new list containing all Action objects</p>"},{"location":"reference/actions/#predicates-effects","title":"Predicates &amp; Effects","text":"<p>Predicates are used in preconditions to check state values; effects define how state changes when an action is applied.</p>"},{"location":"reference/actions/#predicates","title":"Predicates","text":"<p>All predicates support positional arguments and JSON serialization via <code>to_dict()</code> and <code>from_dict()</code>:</p> <pre><code>Range(10, 20)           # Range(min=10, max=20)\nGreaterThan(5)\nLessThan(50)\n\n# Serialization / Deserialization\ngt_dict = GreaterThan(5).to_dict()  # {\"op\": \"gt\", \"value\": 5}\npredicate = Predicate.from_dict(gt_dict)\n</code></pre>"},{"location":"reference/actions/#goapauto.models.actions.Predicate","title":"<code>goapauto.models.actions.Predicate</code>","text":"<p>Base class for state predicates.</p> <p>Predicates are used in preconditions to check if a state value meets certain criteria.</p>"},{"location":"reference/actions/#goapauto.models.actions.Predicate.to_dict","title":"<code>to_dict()</code>","text":"<p>Convert predicate to a serializable dictionary.</p>"},{"location":"reference/actions/#goapauto.models.actions.Predicate.from_dict","title":"<code>from_dict(data)</code>  <code>classmethod</code>","text":"<p>Create a Predicate subclass instance from a dictionary.</p>"},{"location":"reference/actions/#goapauto.models.actions.Predicate.__call__","title":"<code>__call__(value)</code>  <code>abstractmethod</code>","text":"<p>Evaluate the predicate against a value.</p>"},{"location":"reference/actions/#goapauto.models.actions.Equal","title":"<code>goapauto.models.actions.Equal</code>","text":"<p>Predicate that checks if a value is equal to another.</p>"},{"location":"reference/actions/#goapauto.models.actions.Equal.__init__","title":"<code>__init__(value)</code>","text":"<p>Initialize the predicate, supporting positional arguments.</p>"},{"location":"reference/actions/#goapauto.models.actions.NotEqual","title":"<code>goapauto.models.actions.NotEqual</code>","text":"<p>Predicate that checks if a value is not equal to another.</p>"},{"location":"reference/actions/#goapauto.models.actions.NotEqual.__init__","title":"<code>__init__(value)</code>","text":"<p>Initialize the predicate, supporting positional arguments.</p>"},{"location":"reference/actions/#goapauto.models.actions.GreaterThan","title":"<code>goapauto.models.actions.GreaterThan</code>","text":"<p>Predicate that checks if a value is greater than another.</p>"},{"location":"reference/actions/#goapauto.models.actions.GreaterThan.__init__","title":"<code>__init__(value)</code>","text":"<p>Initialize the predicate, supporting positional arguments.</p>"},{"location":"reference/actions/#goapauto.models.actions.LessThan","title":"<code>goapauto.models.actions.LessThan</code>","text":"<p>Predicate that checks if a value is less than another.</p>"},{"location":"reference/actions/#goapauto.models.actions.LessThan.__init__","title":"<code>__init__(value)</code>","text":"<p>Initialize the predicate, supporting positional arguments.</p>"},{"location":"reference/actions/#goapauto.models.actions.Range","title":"<code>goapauto.models.actions.Range</code>","text":"<p>Predicate that checks if a value is within a range (inclusive).</p>"},{"location":"reference/actions/#goapauto.models.actions.Range.__init__","title":"<code>__init__(min_value, max_value)</code>","text":"<p>Initialize the range predicate with inclusive bounds.</p>"},{"location":"reference/actions/#effects","title":"Effects","text":"<p>All effects support positional arguments and JSON serialization via <code>to_dict()</code> and <code>from_dict()</code>:</p> <pre><code>Set(\"kitchen\")          # instead of Set(value=\"kitchen\")\nIncrement(5)            # instead of Increment(amount=5)\nDecrement(3)\nUnset()                 # remove attribute\nDelete()                # alias for Unset\n\n# Serialization / Deserialization\ninc_dict = Increment(5).to_dict()  # {\"op\": \"inc\", \"amount\": 5}\neffect = Effect.from_dict(inc_dict)\n</code></pre>"},{"location":"reference/actions/#goapauto.models.actions.Effect","title":"<code>goapauto.models.actions.Effect</code>","text":"<p>Base class for state effects.</p> <p>Effects are used to define how a state attribute changes when an action is applied.</p>"},{"location":"reference/actions/#goapauto.models.actions.Effect.to_dict","title":"<code>to_dict()</code>","text":"<p>Convert effect to a serializable dictionary.</p>"},{"location":"reference/actions/#goapauto.models.actions.Effect.from_dict","title":"<code>from_dict(data)</code>  <code>classmethod</code>","text":"<p>Create an Effect subclass instance from a dictionary.</p>"},{"location":"reference/actions/#goapauto.models.actions.Effect.__call__","title":"<code>__call__(current_value)</code>  <code>abstractmethod</code>","text":"<p>Compute the new value based on the current value.</p>"},{"location":"reference/actions/#goapauto.models.actions.Set","title":"<code>goapauto.models.actions.Set</code>","text":"<p>Effect that sets an attribute to a specific value.</p>"},{"location":"reference/actions/#goapauto.models.actions.Set.__init__","title":"<code>__init__(value)</code>","text":"<p>Initialize the effect, supporting positional arguments.</p>"},{"location":"reference/actions/#goapauto.models.actions.Increment","title":"<code>goapauto.models.actions.Increment</code>","text":"<p>Effect that increments a numeric attribute.</p>"},{"location":"reference/actions/#goapauto.models.actions.Increment.__init__","title":"<code>__init__(amount=1)</code>","text":"<p>Initialize the effect, supporting positional arguments.</p>"},{"location":"reference/actions/#goapauto.models.actions.Decrement","title":"<code>goapauto.models.actions.Decrement</code>","text":"<p>Effect that decrements a numeric attribute.</p>"},{"location":"reference/actions/#goapauto.models.actions.Decrement.__init__","title":"<code>__init__(amount=1)</code>","text":"<p>Initialize the effect, supporting positional arguments.</p>"},{"location":"reference/actions/#goapauto.models.actions.Unset","title":"<code>goapauto.models.actions.Unset</code>","text":"<p>Effect that removes an attribute from the state.</p>"},{"location":"reference/actions/#goapauto.models.actions.Unset.__init__","title":"<code>__init__()</code>","text":"<p>Initialize the unset effect.</p>"},{"location":"reference/execution/","title":"Execution Interruption API Reference","text":"<p>Cooperative interruption of in-flight plan execution. An interrupt request is honored at the next action boundary -- after the currently-running action (if any) completes and before the next action starts. Actions are atomic: there is no mid-action checkpoint and no preemption of a running handler.</p> <pre><code>from goapauto import Planner, PlanInterruptedError\n\nplanner = Planner(actions_list=[...])\nexecution = planner.begin_execution(state, plan, goal)\n\n# From anywhere with a reference to the handle -- another thread, a hook,\n# a watchdog, the Soulscape tick loop:\nexecution.interrupt(\"enemy_sighted\", source=\"sensor\")\n\ntry:\n    final = execution.run()\nexcept PlanInterruptedError as err:\n    print(err.reason)            # \"enemy_sighted\"\n    print(err.executed_actions)  # [\"move_to_cover\", ...]\n    print(err.next_action)       # action that never started\n    # Replan from the exact partial-state snapshot:\n    new_plan = planner.continue_plan(err.state, goal, list(err.executed_actions))\n</code></pre> <p>The sensor-staleness pull wiring is an <code>on_action_complete</code> closure over the sensor's generation counter that calls <code>execution.interrupt(...)</code> when the world moved. There is deliberately no predicate seam: the handle covers pre-run (flag), per-boundary pull (hook closure), and async watchdogs (threads).</p>"},{"location":"reference/execution/#planexecution","title":"PlanExecution","text":"<p>The execution handle, created via <code>Planner.begin_execution</code>. Single-shot: <code>run()</code> / <code>arun()</code> raise <code>RuntimeError</code> on a second call. <code>interrupt()</code> and the read-only inspection properties are safe to use from another thread; <code>run()</code> / <code>arun()</code> must be called on exactly one thread.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution","title":"<code>goapauto.models.execution.PlanExecution</code>","text":"<p>Handle for one interruptible run of a plan.</p> <p>Created via :meth:<code>Planner.begin_execution &lt;goapauto.models.goap_planner.Planner.begin_execution&gt;</code>. The handle is single-shot: :meth:<code>run</code> / :meth:<code>arun</code> may each be called at most once -- a second call raises <code>RuntimeError</code>.</p> <p>Thread-safety carve-out: the library is otherwise not thread-safe, but :meth:<code>interrupt</code> and the read-only inspection properties are safe to use from another thread (the watchdog pattern). <code>run()</code> / <code>arun()</code> must be called on exactly one thread.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.executed_actions","title":"<code>executed_actions</code>  <code>property</code>","text":"<p>Action-name strings executed so far, in execution order.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.interrupt_reason","title":"<code>interrupt_reason</code>  <code>property</code>","text":"<p>Reason of the first recorded interrupt request, if any.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.interrupt_source","title":"<code>interrupt_source</code>  <code>property</code>","text":"<p>Source of the first recorded interrupt request, if any.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.is_complete","title":"<code>is_complete</code>  <code>property</code>","text":"<p>True iff the run returned normally. Terminal.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.is_interrupted","title":"<code>is_interrupted</code>  <code>property</code>","text":"<p>True iff the run stopped because of an interrupt. Terminal.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.remaining_actions","title":"<code>remaining_actions</code>  <code>property</code>","text":"<p>Action-name strings never started, in plan order.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.state","title":"<code>state</code>  <code>property</code>","text":"<p>Deep-copy snapshot of the current partial state.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.arun","title":"<code>arun()</code>  <code>async</code>","text":"<p>Asynchronous twin of :meth:<code>run</code>.</p> <p>Honors async handlers. If the task running <code>arun()</code> is cancelled, the <code>CancelledError</code> propagates unchanged: the execution is marked interrupted (<code>source=\"cancellation\"</code>) and <code>on_execution_interrupted</code> still fires, but a hook raising on that path is captured and logged rather than superseding the <code>CancelledError</code>. Single-shot like :meth:<code>run</code>.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.interrupt","title":"<code>interrupt(reason, *, source='manual')</code>","text":"<p>Request interruption of this execution.</p> <p>The request is honored at the next action boundary. First call wins: it records <code>(reason, source)</code> and returns <code>True</code>; later calls return <code>False</code> and change nothing. Interrupting a terminal (completed/failed/interrupted) handle is a no-op returning <code>False</code>.</p> <p>Argument validation runs before the terminal no-op check, so API misuse raises <code>ValueError</code> even on a finished handle.</p> <p>Safe to call from another thread.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanExecution.run","title":"<code>run()</code>","text":"<p>Execute the plan synchronously to completion.</p> <p>Returns the final <code>WorldState</code>. Raises :class:<code>PlanInterruptedError</code> when an interrupt is honored; <code>PlanExecutionError</code> / <code>KeyError</code> / hook exceptions propagate exactly as <code>execute_plan</code> does. Single-shot: a second call raises <code>RuntimeError</code>.</p>"},{"location":"reference/execution/#planinterruptederror","title":"PlanInterruptedError","text":"<p>Raised when a run is interrupted. A <code>ValueError</code> sibling of <code>PlanExecutionError</code> -- deliberately not a subclass, so existing <code>except PlanExecutionError</code> failure handlers never swallow a deliberate interruption. Never raised by <code>execute_plan</code> / <code>async_execute_plan</code>.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanInterruptedError","title":"<code>goapauto.models.execution.PlanInterruptedError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Raised when a :class:<code>PlanExecution</code> run is interrupted at an action boundary.</p> <p>A sibling of <code>PlanExecutionError</code> under <code>ValueError</code> -- deliberately not a subclass. Interruption is not failure: existing <code>except PlanExecutionError</code> handlers (log, alert, retry, count) must not swallow deliberate interruptions. Code that wants \"execution did not reach the goal\" uniformly should catch <code>(PlanInterruptedError, PlanExecutionError)</code>, with <code>PlanInterruptedError</code> first when the distinction matters.</p> <p>Never raised by <code>execute_plan</code> / <code>async_execute_plan</code> -- only by :meth:<code>PlanExecution.run</code> / :meth:<code>PlanExecution.arun</code>.</p>"},{"location":"reference/execution/#goapauto.models.execution.PlanInterruptedError.next_action","title":"<code>next_action</code>  <code>property</code>","text":"<p>Name of the next action that never started, or <code>None</code>.</p> <p>Convenience derivation from <code>remaining_actions</code> -- not carried data.</p>"},{"location":"reference/execution/#interruptsource","title":"InterruptSource","text":"<p>Closed literal naming who interrupted: <code>\"manual\"</code>, <code>\"sensor\"</code>, <code>\"watchdog\"</code>, <code>\"budget\"</code>, <code>\"arbitration\"</code>, <code>\"confidence\"</code>, <code>\"cancellation\"</code>. Diagnostics grouping keys on it; the free-form <code>reason</code> carries the human detail.</p>"},{"location":"reference/execution/#goapauto.models.execution.InterruptSource","title":"<code>goapauto.models.execution.InterruptSource = Literal['manual', 'sensor', 'watchdog', 'budget', 'arbitration', 'confidence', 'cancellation']</code>  <code>module-attribute</code>","text":"<p>Who requested an interruption. Closed literal: diagnostics grouping keys on it.</p>"},{"location":"reference/execution/#on_execution_interrupted","title":"on_execution_interrupted","text":"<p>Hook event fired exactly once per interrupted run -- on every interruption path, including the <code>asyncio.CancelledError</code> path -- and then the machinery raises. Never fired alongside <code>on_execution_complete</code> / <code>on_execution_failed</code> for the same run. Exact kwargs: <code>reason</code>, <code>source</code>, <code>state</code>, <code>executed_actions</code>, <code>remaining_actions</code>, <code>step_index</code>, <code>plan_length</code>, <code>state_diff</code>, <code>interrupted_at</code>.</p> <pre><code>planner.register_hook(\n    \"on_execution_interrupted\",\n    lambda *, reason, source, state, executed_actions, remaining_actions,\n             step_index, plan_length, state_diff, interrupted_at: ...,\n)\n</code></pre>"},{"location":"reference/goal/","title":"Goal API Reference","text":"<p>The <code>Goal</code> class represents a target state to achieve.</p> <pre><code>from goapauto.models.goal import Goal\nfrom goapauto.models.actions import GreaterThan, LessThan, Range\n\ngoal = Goal(\n    target_state={\n        \"health\": GreaterThan(50),\n        \"ammo\": Range(10, 100),\n        \"enemy_dead\": True\n    },\n    priority=1, # Lower number = higher priority (must be &gt;= 1)\n    name=\"Survive\"\n)\n</code></pre>"},{"location":"reference/goal/#callable-targets","title":"Callable Targets","text":"<p>Goal targets can be callables (lambdas, predicates) for flexible conditions:</p> <pre><code>goal = Goal(\n    target_state={\n        \"health\": lambda v: v &gt;= 50,      # custom predicate\n        \"ammo\": lambda v: 10 &lt;= v &lt;= 100, # inline lambda\n        \"alive\": True\n    }\n)\n</code></pre>"},{"location":"reference/goal/#range-inequality-goals","title":"Range &amp; Inequality Goals","text":"<p>Use built-in predicates for common numeric constraints:</p> <pre><code># At least 10\n{\"health\": GreaterThan(10)}\n\n# At most 50\n{\"weight\": LessThan(50)}\n\n# Range (inclusive)\n{\"ammo\": Range(10, 100)}\n</code></pre>"},{"location":"reference/goal/#goapauto.models.goal.Goal","title":"<code>goapauto.models.goal.Goal</code>","text":"<p>Represents a goal with a target state and optional priority.</p> <p>A goal defines a desired state that the planner should try to achieve. Goals can have different priorities, with lower numbers indicating higher priority (e.g., priority 1 is higher than priority 2).</p>"},{"location":"reference/goal/#goapauto.models.goal.Goal.__hash__","title":"<code>__hash__()</code>","text":"<p>Compute a hash value for this goal.</p>"},{"location":"reference/goal/#goapauto.models.goal.Goal.__repr__","title":"<code>__repr__()</code>","text":"<p>Return a detailed string representation of the goal.</p>"},{"location":"reference/goal/#goapauto.models.goal.Goal.__str__","title":"<code>__str__()</code>","text":"<p>Return a string representation of the goal.</p>"},{"location":"reference/goal/#goapauto.models.goal.Goal.get_unsatisfied_conditions","title":"<code>get_unsatisfied_conditions(world_state)</code>","text":"<p>Get the conditions that are not satisfied in the current world state.</p> <p>A key missing from the world state is always reported as unsatisfied.</p>"},{"location":"reference/goal/#goapauto.models.goal.Goal.is_satisfied","title":"<code>is_satisfied(world_state)</code>","text":"<p>Check if this goal is satisfied by the given world state.</p> <p>A key missing from the world state never satisfies a condition -- missing keys fail goal checks exactly like they fail action preconditions. Predicate errors propagate to the caller.</p> <p>Parameters:</p> Name Type Description Default <code>world_state</code> <code>Any</code> <p>The world state to check against.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all conditions in target_state are satisfied.</p>"},{"location":"reference/goal/#goapauto.models.goal.Goal.model_post_init","title":"<code>model_post_init(__context)</code>","text":"<p>Initialize name if not provided.</p>"},{"location":"reference/inspector/","title":"Inspector API Reference","text":"<p>The <code>AgentInspector</code> is a passive \"why this goal / plan / action?\" diagnostics recorder. It subscribes to the planner and replan-policy hooks, ingests Jev/Judgment telemetry through explicit callbacks, and answers what the agent decided, why, and what changed between ticks.</p> <pre><code>from goapauto.utils.inspector import AgentInspector\n\ninspector = AgentInspector()\ninspector.attach(planner, arbitrator=arbitrator, sensor_manager=sensors)\n\nfor tick in loop:\n    inspector.mark_tick(state)\n    goal = arbitrator.select_goal(state)\n    result = planner.generate_plan(state, goal)\n    ...\n    delta = inspector.what_changed_since(tick)\n\nprint(inspector.to_markdown())\ninspector.to_jsonl(\"trace.jsonl\")\ninspector.detach()\n</code></pre> <p>The inspector never mutates the agent: hook callbacks are removed with the same list objects on <code>detach()</code>, and every ingestion method is total \u2014 a malformed record is captured as a diagnostic event, never raised.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector","title":"<code>goapauto.utils.inspector.AgentInspector</code>","text":"<p>Passive \"why this goal / plan / action?\" diagnostics recorder.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.attach","title":"<code>attach(planner, *, arbitrator=None, sensor_manager=None, replan_policy=None, max_events=4096, max_report_events=200, keep_ticks=2)</code>","text":"<p>Subscribe to planner and replan-policy hooks and start recording.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.buffer_info","title":"<code>buffer_info()</code>","text":"<p>Return trace buffer retention statistics.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.detach","title":"<code>detach()</code>","text":"<p>Remove hook callbacks and stop recording; the buffer is retained.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.mark_tick","title":"<code>mark_tick(state, label=None)</code>","text":"<p>Record a tick boundary with a deep snapshot of the world state.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.note_goal_selected","title":"<code>note_goal_selected(*, selected, candidates, strategy_name, strategy_record_name=None)</code>","text":"<p>Record one goal arbitration outcome.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.note_jev_call","title":"<code>note_jev_call(name, record)</code>","text":"<p>Ingest one Jev/Judgment telemetry record. Never raises.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.note_sensor_update","title":"<code>note_sensor_update(*, sensor, updates, duration_ms, keys=None, error=None)</code>","text":"<p>Record one explicit sensor update. Never raises.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.record_search_error","title":"<code>record_search_error(error)</code>","text":"<p>Record a search exception the caller re-raises after this call.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.record_sensor_error","title":"<code>record_sensor_error(sensor, error)</code>","text":"<p>Record a sensor exception the caller re-raises after this call.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.shim_strategy","title":"<code>shim_strategy(strategy, *, name)</code>","text":"<p>Wrap a goal-selection strategy so selections are recorded.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.timeline","title":"<code>timeline(*, types=None, since_seq=0, limit=None)</code>","text":"<p>Return retained events, newest last, with optional filters.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.to_jsonl","title":"<code>to_jsonl(path)</code>","text":"<p>Write one JSON object per retained event to <code>path</code>.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.to_markdown","title":"<code>to_markdown(max_report_events=None)</code>","text":"<p>Render the why-report as Markdown.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.what_changed_since","title":"<code>what_changed_since(tick_id)</code>","text":"<p>Diff the world state and trace between a retained tick and now.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.why_goal","title":"<code>why_goal()</code>","text":"<p>Return the latest recorded goal arbitration outcome.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.AgentInspector.why_plan","title":"<code>why_plan()</code>","text":"<p>Return the latest recorded plan outcome.</p>"},{"location":"reference/inspector/#query-results","title":"Query Results","text":"<p><code>why_goal()</code> and <code>why_plan()</code> return typed dataclasses; <code>what_changed_since()</code> returns a <code>TickDelta</code>:</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.GoalDecision","title":"<code>goapauto.utils.inspector.GoalDecision</code>  <code>dataclass</code>","text":"<p>The latest recorded goal arbitration outcome.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.PlanDecision","title":"<code>goapauto.utils.inspector.PlanDecision</code>  <code>dataclass</code>","text":"<p>The latest recorded plan outcome.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.TickDelta","title":"<code>goapauto.utils.inspector.TickDelta</code>  <code>dataclass</code>","text":"<p>What changed between two retained tick snapshots.</p>"},{"location":"reference/inspector/#trace-events","title":"Trace Events","text":"<p>Every retained record is a frozen <code>TraceEvent</code> with a sequence number, optional tick, monotonic timestamp, and JSON-safe data payload:</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.TraceEvent","title":"<code>goapauto.utils.inspector.TraceEvent</code>  <code>dataclass</code>","text":"<p>One retained trace record.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.TraceEvent.to_dict","title":"<code>to_dict()</code>","text":"<p>Return this event as a JSON-safe dict.</p>"},{"location":"reference/inspector/#goapauto.utils.inspector.EventType","title":"<code>goapauto.utils.inspector.EventType = Literal['tick_boundary', 'goal_selected', 'plan_found', 'plan_failed', 'search_error', 'action_started', 'action_completed', 'action_failed', 'execution_failed', 'execution_interrupted', 'replan', 'replan_throttled', 'budget_exhausted', 'judgment_gated', 'sensor_update', 'sensor_error']</code>  <code>module-attribute</code>","text":""},{"location":"reference/jev/","title":"TypeSafe Judgments","text":"<p><code>JevSensor</code> and <code>JevGoalStrategy</code> plug TypeSafe System One judgments into the two places where GOAP meets fuzzy reality: perception and goal selection. They live behind the optional <code>jev</code> extra:</p> <pre><code>uv add \"goapauto[jev]\"\n</code></pre> <p>Without the extra, importing <code>goapauto</code> still works \u2014 only the <code>Jev*</code> symbols (resolved lazily) raise a helpful error telling you to install it.</p> <p>A <code>JevSensor</code> sends a raw observation as TypeSafe state and maps the typed answers onto world-state keys (<code>noul</code> \u2192 probability, <code>choice</code> \u2192 label, <code>score</code> \u2192 rubric value). The planner keeps reasoning over crisp values; only the perception step uses the model. Judgments are cached and re-queried when the observation changes or <code>min_interval</code> elapses.</p> <pre><code>from typesafe_sdk import Noul\n\nfrom goapauto import JevSensor, SensorManager, WorldState\n\nsensor = JevSensor(\n    observe=lambda: {\"enemies_nearby\": 3, \"health\": 20},\n    questions={\n        \"danger\": Noul(instructions=\"Is the agent in immediate danger?\"),\n    },\n    min_interval=5.0,\n)\nstate = WorldState()\nSensorManager(sensors=[sensor]).update_state(state)\nprint(state.danger)  # e.g. 0.87\n</code></pre> <p>A <code>JevGoalStrategy</code> implements the <code>GoalSelectionStrategy</code> protocol: it asks Jev which active goal to pursue, choosing between the goals by name. Both take the official <code>typesafe-sdk</code> <code>TypeSafeClient</code> directly \u2014 no wrapper.</p> <pre><code>from typesafe_sdk import TypeSafeClient\n\nfrom goapauto import Goal, GoalArbitrator, JevGoalStrategy\n\nclient = TypeSafeClient()\narbitrator = GoalArbitrator(\n    goals=[Goal(target_state={\"fed\": True}, name=\"Eat\")],\n    strategy=JevGoalStrategy(client=client),\n)\n</code></pre> <p>Both need a TypeSafe API key (<code>TYPESAFE_API_KEY</code>). On failure they degrade gracefully: the sensor reuses its last judgments, the strategy falls back to the first goal.</p>"},{"location":"reference/jev/#injecting-observations","title":"Injecting observations","text":"<p><code>sense()</code> pulls an observation through the <code>observe</code> callback. When the host builds observations on its own thread (a game loop holding DB access, say) and hands the snapshot to a worker thread that owns the client, use <code>judge(observation)</code> instead \u2014 same cache, staleness, and telemetry, but the observation is supplied by the caller. <code>sense()</code> and <code>judge()</code> share cache state, so don't mix them on one sensor.</p>"},{"location":"reference/jev/#sharing-one-client","title":"Sharing one client","text":"<p>By default each class constructs and owns its own <code>TypeSafeClient</code>. To share one client (one connection pool, one timeout config), use <code>shared_client</code>:</p> <pre><code>from goapauto import JevGoalStrategy, JevSensor, shared_client\n\nwith shared_client(timeout=8.0) as client:\n    sensor = JevSensor(observe=..., questions=..., client=client)\n    strategy = JevGoalStrategy(client=client)\n</code></pre> <p>A client passed explicitly keeps its caller-owned lifecycle; only defaulted clients are closed by their owner.</p>"},{"location":"reference/jev/#telemetry","title":"Telemetry","text":"<p>Pass <code>telemetry=</code> a callback receiving a <code>JevCallRecord</code> per attempted call (source, latency, token usage, error, stale-cache hit), or poll <code>stats()</code> for aggregate counters. Both are plain data \u2014 no logging framework or vendor lock-in \u2014 so a host can calibrate thresholds from real judgments:</p> <pre><code>records = []\nsensor = JevSensor(observe=..., questions=..., telemetry=records.append)\n...\nprint(sensor.stats())  # JevStats(calls=..., errors=..., ...)\n</code></pre>"},{"location":"reference/jev/#offline-testing","title":"Offline testing","text":"<p><code>goapauto.testing.FakeTypeSafeClient</code> answers with canned values \u2014 no key, no network \u2014 for tests and <code>--demo</code> runs. See <code>examples/jev_reference.py</code> for the canonical loop.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevSensor","title":"<code>goapauto.models.jev.JevSensor</code>","text":"<p>World-state sensor backed by TypeSafe judgments.</p> <p>Classical sensors hand-code thresholds (<code>danger = enemies_nearby &gt; 3</code>). A JevSensor instead sends a raw observation as TypeSafe state and maps the typed answers onto world-state keys:</p> <ul> <li><code>noul</code> answers become floats in <code>[0, 1]</code></li> <li><code>choice</code> answers become the selected label</li> <li><code>score</code> answers become floats on the question's rubric</li> </ul> <p>The planner keeps reasoning over crisp values; only the perception step uses the model. Judgments are cached: the API is re-queried when the observation changes (if <code>resense_on_change</code>) or once <code>min_interval</code> seconds have passed. On failure the last good judgments are reused so the agent loop keeps running -- but only for <code>max_stale</code> seconds; after that the cache is considered dead and <code>sense()</code> returns no updates (degrading visibly instead of acting on ancient judgments).</p> <p><code>stale_after</code> opts into the staleness ladder: keys age per-key through FRESH/STALE/DEAD bands (<code>stale_policy</code> decides whether STALE keys are kept flagged or dropped) instead of the legacy whole-cache cliff. The default <code>None</code> keeps the 0.5.0 behavior exactly. <code>sense_detailed()</code>, <code>health()</code>, <code>diagnostics()</code> and <code>last_report()</code> expose the per-key provenance; <code>observation_fingerprint</code> is an optional change-detector used instead of raw observation comparison.</p> <p>Questions must be official <code>typesafe-sdk</code> question objects (<code>Noul</code>, <code>Choice</code>, <code>Score</code>); raw dictionaries are rejected with <code>TypeError</code>. The client is the SDK's <code>TypeSafeClient</code> directly.</p> <p>Thread safety: not thread-safe. Use one JevSensor per thread; do not share it across threads without external synchronization.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevSensor.close","title":"<code>close()</code>","text":"<p>Close the TypeSafe client if this sensor created it.</p> <p>Releases the underlying HTTP connection pool. A client passed in by the caller is left open; its lifecycle belongs to the caller.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevSensor.diagnostics","title":"<code>diagnostics()</code>","text":"<p>Queryable sensor summary for hosts; never raises.</p> <p>Merges the 02 caching diagnostics (\u00a79.4: <code>data_age_seconds</code>, <code>staleness</code>, <code>dead_keys</code>, <code>worst_staleness</code>) with the 04 confidence-gating summary (\u00a75e). Design 06 consumes both key sets from this one mapping. Before the first report only the gating keys appear \u2014 there is no cache data yet.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevSensor.health","title":"<code>health()</code>","text":"<p>Current band/health view over the cache; never raises.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevSensor.judge","title":"<code>judge(observation)</code>","text":"<p>Judge an injected observation instead of pulling one via <code>observe</code>.</p> <p>Same cache, staleness, and telemetry semantics as <code>sense()</code>; the only difference is where the observation comes from. This lets a host build the observation on its own thread (e.g. holding DB access) and hand the snapshot to a worker thread that owns the client.</p> <p><code>sense()</code> and <code>judge()</code> share cache state: do not mix them on one instance.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevSensor.last_report","title":"<code>last_report()</code>","text":"<p>Most recent report, or None before the first report-producing call.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevSensor.sense","title":"<code>sense()</code>","text":"<p>Return judgment-derived state updates, re-querying only when due.</p> <p>When a re-judge fails, the last good judgments are reused only while they are younger than <code>max_stale</code> seconds; older than that, the cache is dead and an empty dict is returned so the agent degrades visibly instead of acting on stale perception.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevSensor.sense_detailed","title":"<code>sense_detailed()</code>","text":"<p>Like :meth:<code>sense</code>, with per-key age and staleness metadata.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevSensor.stats","title":"<code>stats()</code>","text":"<p>Return aggregate telemetry for this sensor's TypeSafe calls.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevGoalStrategy","title":"<code>goapauto.models.jev.JevGoalStrategy</code>","text":"<p>GoalSelectionStrategy that asks Jev which active goal to pursue.</p> <p>Builds a <code>choice</code> question with one labeled option per goal (the goal name, described by its target state and priority) and selects the goal Jev picks. Satisfied goals never reach the model: <code>GoalArbitrator</code> filters them before calling <code>select</code>.</p> <p>Goal names must be unique. If the model is unreachable or names an unknown goal, the first goal is returned so the agent loop keeps running.</p> <p>Thread safety: not thread-safe. Use one JevGoalStrategy per thread; do not share it across threads without external synchronization.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevGoalStrategy.close","title":"<code>close()</code>","text":"<p>Close the TypeSafe client if this strategy created it.</p> <p>Releases the underlying HTTP connection pool. A client passed in by the caller is left open; its lifecycle belongs to the caller.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevGoalStrategy.select","title":"<code>select(goals, state)</code>","text":"<p>Select the goal Jev judges most worth pursuing right now.</p> <p>When <code>confidence_threshold</code> is set, a pick reported below the threshold is gated: the strategy falls back via <code>fallback_policy</code> instead of trusting the pick. Corrupt reported confidence (NaN, out of [0, 1]) is fail-loud: it is recorded and re-raised as <code>TypeSafeError</code>, never treated as a low-confidence pick. With <code>confidence_threshold=None</code> (the default) this is byte-identical to the 0.5.0 behavior.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevGoalStrategy.stats","title":"<code>stats()</code>","text":"<p>Return aggregate telemetry for this strategy's TypeSafe calls.</p>"},{"location":"reference/jev/#goapauto.models.jev.JevJudge","title":"<code>goapauto.models.jev.JevJudge</code>","text":"<p>A <code>Judge</code> backend over a <code>TypeSafeClient</code> (the Jev provider).</p> <p>Translates the provider-independent questions of <code>goapauto.models.judgment</code> to SDK questions and back. A core question whose <code>metadata</code> carries the exact SDK question under <code>\"jev.question\"</code> is forwarded verbatim; the rest are converted mechanically (<code>ScoreQuestion</code> via the canonical rung ladder below).</p> <p>Client ownership mirrors <code>JevSensor</code>: a client passed in stays caller-owned; the default client is owned here and closed idempotently. No exception named <code>TypeSafeError</code> ever crosses <code>judge</code>: provider failures and bad answers surface as <code>JudgmentError</code> chained from the original.</p>"},{"location":"reference/judgment/","title":"Provider-Independent Judgment","text":"<p>The judgment layer answers fuzzy questions about the world \u2014 is the agent in danger?, which goal next? \u2014 through any backend implementing the <code>Judge</code> protocol. Core (<code>goapauto</code>, no third-party imports) defines the question/answer types, the protocol, the error contract, and two generic layers; <code>JevJudge</code> (in <code>goapauto.models.jev</code>, behind the <code>jev</code> extra) adapts TypeSafe System One to it.</p>"},{"location":"reference/judgment/#questions-and-answers","title":"Questions and answers","text":"<p>Three frozen question types, deliberately Jev-shaped. Every question carries an optional <code>metadata</code> side-channel that generic layers never interpret \u2014 backends use it for round-trip identity (e.g. <code>metadata[\"jev.question\"]</code> holds the original SDK question, reused verbatim by <code>JevJudge</code> so the model prompt is byte-identical).</p> <pre><code>from goapauto import ChoiceQuestion, NoulQuestion, ScoreQuestion\n\nquestions = {\n    \"danger\": NoulQuestion(\"Is the agent in immediate danger?\"),\n    \"threat\": ChoiceQuestion(\n        \"What is the nearest creature doing?\",\n        choices=(\"hunting\", \"sleeping\"),\n    ),\n    \"hunger\": ScoreQuestion(\"How urgent is food?\", min_value=0.0, max_value=2.0),\n}\n</code></pre> <p>Answers come back as <code>FloatAnswer(value, confidence)</code> for <code>Noul</code>/<code>Score</code> questions and <code>ChoiceAnswer(value, confidence)</code> for <code>Choice</code> questions. Confidence is the backend's reported value, or <code>None</code> when it reports none \u2014 never synthesized, never clamped. Out-of-range values raise <code>JudgmentError(retryable=False)</code> instead of being clamped.</p>"},{"location":"reference/judgment/#the-judge-protocol","title":"The <code>Judge</code> protocol","text":"<pre><code>from goapauto import JudgmentError, JudgmentResponse\n\nclass MyJudge:\n    backend = \"mine\"\n\n    def judge(self, state, questions) -&gt; JudgmentResponse: ...\n    def close(self) -&gt; None: ...\n</code></pre> <p><code>judge</code> is synchronous: a hung backend blocks the sense\u2192plan pipeline for its full transport timeout. The only exception generic layers catch is <code>JudgmentError(message, backend=..., retryable=...)</code> \u2014 <code>retryable</code> has no default, every raise site decides. Anything else propagates unchanged (loud by design). Telemetry names the cause chain's class, so a chained <code>TypeSafeError</code> still reports as <code>\"TypeSafeError\"</code>.</p>"},{"location":"reference/judgment/#judgmentsensor","title":"<code>JudgmentSensor</code>","text":"<p>A <code>Sensor</code> over any <code>Judge</code>, with the same cache/staleness/telemetry contract as <code>JevSensor</code>: re-judges on a changed observation (when <code>resense_on_change</code>) or an elapsed <code>min_interval</code>; absorbs retryable failures into the stale-cache path (<code>max_stale=0</code> disables reuse); re-raises non-retryable failures.</p> <pre><code>from goapauto import JudgmentSensor\nfrom goapauto.models.jev import JevJudge\n\nsensor = JudgmentSensor(\n    JevJudge(),  # defaults to TypeSafeClient(timeout=30.0)\n    observe=lambda: {\"enemies_nearby\": 3, \"health\": 20},\n    questions=questions,\n    min_interval=5.0,\n)\nprint(sensor.sense())  # {\"danger\": 0.87, \"threat\": \"hunting\", \"hunger\": 1.5}\n</code></pre> <p><code>sense()</code> and <code>judge(observation)</code> share one cache \u2014 don't mix them on one instance. Not thread-safe: one instance per thread.</p>"},{"location":"reference/judgment/#judgmentgoalstrategy","title":"<code>JudgmentGoalStrategy</code>","text":"<p>Asks the judge one <code>Choice</code> question over the goal names and returns the chosen goal. On retryable failure (or an unknown label) it warns and falls back to the first goal; on non-retryable failure it re-raises <code>JudgmentError</code>.</p> <pre><code>from goapauto import Goal, GoalArbitrator, JudgmentGoalStrategy\nfrom goapauto.models.jev import JevJudge\n\narbitrator = GoalArbitrator(\n    goals=[Goal(target_state={\"fed\": True}, name=\"Eat\")],\n    strategy=JudgmentGoalStrategy(JevJudge()),\n)\n</code></pre>"},{"location":"reference/judgment/#telemetry","title":"Telemetry","text":"<p>Pass <code>telemetry=</code> a callback receiving a <code>JudgmentCallRecord</code> per attempted call \u2014 source, backend, latency, token usage, error, stale-cache hit \u2014 or poll <code>stats()</code> for aggregate counters. Token counts are copied from the backend's response; interpretation is the callback's job.</p>"},{"location":"reference/judgment/#offline-testing","title":"Offline testing","text":"<p><code>goapauto.testing.FakeJudge</code> answers from canned values or a responder \u2014 no <code>typesafe_sdk</code> import anywhere near it:</p> <pre><code>from goapauto import ChoiceAnswer, FloatAnswer\nfrom goapauto.testing import FakeJudge\n\njudge = FakeJudge(\n    answers={\"danger\": FloatAnswer(0.87), \"threat\": ChoiceAnswer(\"hunting\")}\n)\n</code></pre>"},{"location":"reference/judgment/#goapauto.models.judgment.Judge","title":"<code>goapauto.models.judgment.Judge</code>","text":"<p>Anything that can answer judgment questions.</p> <p><code>judge</code> is synchronous: a hung backend blocks the sense\u2192plan pipeline for its full transport timeout. There is no in-flight hook in Phase 1; <code>latency_ms</code> on the telemetry record is post-call attribution only. The protocol guarantees nothing about caching \u2014 a bare <code>judge</code> call always goes to the backend; <code>JudgmentSensor</code> adds the caching layer.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.Judge.close","title":"<code>close()</code>","text":"<p>Release any resources held by this judge.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.Judge.judge","title":"<code>judge(state, questions)</code>","text":"<p>Answer <code>questions</code> about <code>state</code>.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.JudgmentError","title":"<code>goapauto.models.judgment.JudgmentError</code>","text":"<p>A judgment call failed. <code>retryable</code> has no default: callers must say.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.JudgmentSensor","title":"<code>goapauto.models.judgment.JudgmentSensor</code>","text":"<p>Sense the world through any <code>Judge</code> backend.</p> <p>Cache behavior mirrors the frozen 0.5.0 JevSensor exactly: the first call judges; a changed observation (when <code>resense_on_change</code>) or an elapsed <code>min_interval</code> re-judges; <code>min_interval=0</code> re-judges every call; the cache updates atomically after success; <code>sense()</code> and <code>judge()</code> share one cache. Not thread-safe.</p> <p><code>stale_after</code> opts into the per-key staleness ladder (design 02 \u00a73.1) instead of the legacy whole-cache cliff: keys age through FRESH/STALE/DEAD and <code>stale_policy</code> decides whether STALE keys are kept flagged or dropped. <code>None</code> (the default) keeps the 0.5.0 behavior exactly. <code>sense_detailed()</code>, <code>health()</code>, <code>diagnostics()</code> and <code>last_report()</code> expose the per-key provenance; <code>name</code> labels the sensor in reports; <code>observation_fingerprint</code> is an optional change-detector used instead of raw observation comparison.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.JudgmentSensor.diagnostics","title":"<code>diagnostics()</code>","text":"<p>Exact design \u00a76 payload; <code>{}</code> before the first report.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.JudgmentSensor.health","title":"<code>health()</code>","text":"<p>Current band/health view over the cache; never raises.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.JudgmentSensor.judge","title":"<code>judge(observation)</code>","text":"<p>Judge an explicit observation, bypassing the resense check.</p> <p>Shares cache state with <code>sense()</code> \u2014 do not mix them on one instance.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.JudgmentSensor.last_report","title":"<code>last_report()</code>","text":"<p>Most recent report, or None before the first report-producing call.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.JudgmentSensor.sense","title":"<code>sense()</code>","text":"<p>Observe, then judge only when the cache policy requires it.</p> <p>Shares cache state with <code>judge()</code> \u2014 do not mix them on one instance.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.JudgmentSensor.sense_detailed","title":"<code>sense_detailed()</code>","text":"<p>Like :meth:<code>sense</code>, with per-key age and staleness metadata.</p>"},{"location":"reference/judgment/#goapauto.models.judgment.JudgmentGoalStrategy","title":"<code>goapauto.models.judgment.JudgmentGoalStrategy</code>","text":"<p>Pick a goal by asking any <code>Judge</code> backend one Choice question.</p> <p>Retryable failures warn and fall back to the first goal; non-retryable failures re-raise <code>JudgmentError</code>.</p>"},{"location":"reference/perception/","title":"Perception &amp; Arbitration API Reference","text":""},{"location":"reference/perception/#sensors","title":"Sensors","text":"<p><code>Sensor</code> and <code>SensorManager</code> provide dynamic environment perception and state updates.</p> <pre><code>from goapauto.models.sensors import Sensor, SensorManager\n\nclass MySensor(Sensor):\n    def sense(self) -&gt; dict:\n        return {\"health\": 100}\n\nmanager = SensorManager()\nmanager.add_sensor(MySensor())\nmanager.update_state(state)\n</code></pre>"},{"location":"reference/perception/#goapauto.models.sensors.Sensor","title":"<code>goapauto.models.sensors.Sensor</code>","text":"<p>Base class for environment sensors.</p> <p>Sensors are used to perceive the environment and update the WorldState with current data.</p>"},{"location":"reference/perception/#goapauto.models.sensors.Sensor.sense","title":"<code>sense()</code>  <code>abstractmethod</code>","text":"<p>Perceive the environment.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of state updates</p>"},{"location":"reference/perception/#goapauto.models.sensors.SensorManager","title":"<code>goapauto.models.sensors.SensorManager</code>","text":"<p>Manages a collection of sensors and updates WorldState.</p> <p>Thread safety: not thread-safe. Use one SensorManager per thread; do not share it across threads without external synchronization.</p>"},{"location":"reference/perception/#goapauto.models.sensors.SensorManager.add_sensor","title":"<code>add_sensor(sensor)</code>","text":"<p>Add a sensor to the manager.</p>"},{"location":"reference/perception/#goapauto.models.sensors.SensorManager.aupdate_state","title":"<code>aupdate_state(state)</code>  <code>async</code>","text":"<p>Async :meth:<code>update_state</code>.</p> <p>Sensors implementing <code>asense()</code> are awaited; the rest are read via their sync <code>sense()</code> inline -- never <code>to_thread</code> (design \u00a77).</p>"},{"location":"reference/perception/#goapauto.models.sensors.SensorManager.aupdate_state_detailed","title":"<code>aupdate_state_detailed(state)</code>  <code>async</code>","text":"<p>Async :meth:<code>update_state_detailed</code>; same per-sensor preference.</p>"},{"location":"reference/perception/#goapauto.models.sensors.SensorManager.health","title":"<code>health()</code>","text":"<p>Health of every sensor; never raises.</p> <p>Sensors implementing <code>health()</code> report their own bands; plain sensors are always FRESH. A sensor whose <code>health()</code> itself raises is reported as dead with the error recorded.</p>"},{"location":"reference/perception/#goapauto.models.sensors.SensorManager.update_state","title":"<code>update_state(state)</code>","text":"<p>Update the given state with data from all sensors.</p> <p>Sensor errors propagate to the caller -- a broken sensor fails loudly instead of being silently skipped.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>WorldState</code> <p>The current world state</p> required <p>Returns:</p> Type Description <code>WorldState</code> <p>The updated world state (modified in-place or new copy depending on implementation)</p>"},{"location":"reference/perception/#goapauto.models.sensors.SensorManager.update_state_detailed","title":"<code>update_state_detailed(state)</code>","text":"<p>Update state like :meth:<code>update_state</code>, also returning per-key reports.</p> <p>Sensors implementing <code>sense_detailed()</code> contribute their real cache provenance; plain sensors get a synthesized FRESH report (they compute live on every update, so their values are fresh by construction). Reports follow the same merge order as :meth:<code>update_state</code>.</p>"},{"location":"reference/perception/#goal-arbitration","title":"Goal Arbitration","text":"<p><code>GoalArbitrator</code> selects the highest priority satisfiable goal from a list.</p> <pre><code>from goapauto.models.goal_arbitrator import GoalArbitrator\n\narbitrator = GoalArbitrator(goals=[goal1, goal2])\nselected_goal = arbitrator.select_goal(state)\n</code></pre> <p>The <code>GoalArbitrator</code> accepts a custom <code>GoalSelectionStrategy</code> in its constructor. The default is <code>PriorityGoalStrategy</code> (lowest priority number wins).</p>"},{"location":"reference/perception/#goapauto.models.goal_arbitrator.GoalArbitrator","title":"<code>goapauto.models.goal_arbitrator.GoalArbitrator</code>","text":"<p>Manages multiple goals and selects the best one to pursue.</p> <p>The arbitrator uses a selection strategy to decide which goal the agent should focus on based on the current world state.</p> <p>Thread safety: not thread-safe. Use one GoalArbitrator per thread; do not share it across threads without external synchronization.</p>"},{"location":"reference/perception/#goapauto.models.goal_arbitrator.GoalArbitrator.add_goal","title":"<code>add_goal(goal)</code>","text":"<p>Add a goal to the arbitrator.</p>"},{"location":"reference/perception/#goapauto.models.goal_arbitrator.GoalArbitrator.remove_goal","title":"<code>remove_goal(name)</code>","text":"<p>Remove a goal by name.</p>"},{"location":"reference/perception/#goapauto.models.goal_arbitrator.GoalArbitrator.select_goal","title":"<code>select_goal(state)</code>","text":"<p>Select the best goal to pursue using the current strategy.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>WorldState</code> <p>The current world state</p> required <p>Returns:</p> Type Description <code>Goal | None</code> <p>The selected Goal or None</p>"},{"location":"reference/perception/#goapauto.models.goal_arbitrator.GoalSelectionStrategy","title":"<code>goapauto.models.goal_arbitrator.GoalSelectionStrategy</code>","text":"<p>Protocol for goal selection strategies.</p>"},{"location":"reference/perception/#goapauto.models.goal_arbitrator.GoalSelectionStrategy.select","title":"<code>select(goals, state)</code>","text":"<p>Select the best goal to pursue.</p>"},{"location":"reference/perception/#goapauto.models.goal_arbitrator.PriorityGoalStrategy","title":"<code>goapauto.models.goal_arbitrator.PriorityGoalStrategy</code>","text":"<p>Selects the goal with the highest priority (lowest number).</p>"},{"location":"reference/planner/","title":"Planner API Reference","text":"<p>The <code>Planner</code> class is the A* search engine used to find and execute plans.</p> <pre><code>from goapauto.models.goap_planner import Planner\n\nplanner = Planner(\n    actions_list=[...],\n    max_iterations=1000,\n    heuristic_fn=custom_heuristic,\n    verbose=True,          # print progress (default: True)\n    logger=logger,         # custom logging.Logger\n    cost_weights={         # weights for multi-dimensional costs\n        \"time\": 1.0,\n        \"energy\": 0.5\n    }\n)\nresult = planner.generate_plan(start_state, goal)\n</code></pre>"},{"location":"reference/planner/#plan-execution-handlers","title":"Plan Execution &amp; Handlers","text":"<p>The planner can execute generated plans step-by-step with custom execution handlers:</p> <pre><code># Register custom execution handler for an action\ndef handle_open_door(state, action):\n    state.is_open = True\n    return state\n\nplanner.register_execution_handler(\"open_door\", handle_open_door)\n\n# Execute plan synchronously or asynchronously\nfinal_state = planner.execute_plan(initial_state, result)\n# or: await planner.async_execute_plan(initial_state, result)\n</code></pre>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner","title":"<code>goapauto.models.goap_planner.Planner</code>","text":"<p>Goal-Oriented Action Planner (GOAP) implementation using A* search.</p> <p>This class implements a planning system that finds a sequence of actions to achieve a goal state from an initial state, using A* search with a heuristic.</p> <p>The default search is Dijkstra's algorithm (h=0): always admissible, so the first plan found is optimal. Pass <code>heuristic_fn</code> for guided (but only optimal if admissible) search.</p> <p>Planning errors propagate to the caller -- invalid inputs, broken predicates, failing providers, and broken hooks raise instead of returning an empty result. A <code>PlanResult</code> with <code>plan=None</code> means \"no plan found\", not \"an error occurred\".</p> <p>Thread safety: not thread-safe. Use one Planner per thread; do not share it across threads without external synchronization.</p> <p>Attributes:</p> Name Type Description <code>providers</code> <p>List of ActionProvider instances</p> <code>max_iterations</code> <p>Maximum number of iterations before giving up</p> <code>stats</code> <p>Statistics about the last planning operation</p> <code>verbose</code> <p>Whether to print progress to stdout</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.__init__","title":"<code>__init__(actions_list=None, providers=None, max_iterations=1000, heuristic_fn=None, verbose=True, logger=None, cost_weights=None)</code>","text":"<p>Initialize the planner with optional actions, providers, and config.</p> <p>Parameters:</p> Name Type Description Default <code>actions_list</code> <code>list[tuple[str, dict[str, Any], dict[str, Any], float]] | None</code> <p>Optional list of action tuples (name, preconditions, effects, cost)</p> <code>None</code> <code>providers</code> <code>list[ActionProvider] | None</code> <p>Optional list of ActionProvider instances</p> <code>None</code> <code>max_iterations</code> <code>int</code> <p>Maximum number of iterations for the search algorithm</p> <code>1000</code> <code>heuristic_fn</code> <code>HeuristicFn | None</code> <p>Optional default heuristic function. When omitted the search runs Dijkstra's algorithm (h=0), which is always optimal; a custom function must be admissible to keep that guarantee.</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>Whether to print progress messages to stdout (default True)</p> <code>True</code> <code>logger</code> <code>Logger | None</code> <p>Optional custom logger instance (uses module logger if None)</p> <code>None</code> <code>cost_weights</code> <code>dict[str, float] | list[float] | None</code> <p>Optional weights for multi-dimensional costs. If dict: keys match action cost dict keys (e.g., {\"time\": 1.0, \"energy\": 0.5}) If list: weights correspond to cost list indices If None: actions must use scalar costs</p> <code>None</code>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.async_execute_plan","title":"<code>async_execute_plan(initial_state, plan, goal=None)</code>  <code>async</code>","text":"<p>Asynchronously execute a sequence of actions on the initial state.</p> <p>Parameters:</p> Name Type Description Default <code>initial_state</code> <code>WorldState</code> <p>Starting WorldState</p> required <code>plan</code> <code>Plan | list[Action] | PlanResult</code> <p>Sequence of action names, Action objects, or a PlanResult</p> required <code>goal</code> <code>Goal | None</code> <p>Optional Goal context for dynamic action providers</p> <code>None</code> <p>Returns:</p> Type Description <code>WorldState</code> <p>WorldState after executing all plan steps</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If initial_state/plan has an invalid type</p> <code>ValueError</code> <p>If PlanResult.plan is None</p> <code>KeyError</code> <p>If an action name is not found in registered providers</p> <code>PlanExecutionError</code> <p>If an action's preconditions are not met</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.async_generate_plan","title":"<code>async_generate_plan(world_state, goal, max_depth=None, heuristic_fn=None, time_budget=None)</code>  <code>async</code>","text":"<p>Asynchronously generate a plan.</p> <p>Parameters:</p> Name Type Description Default <code>world_state</code> <code>dict[str, Any] | WorldState</code> <p>The current state of the world</p> required <code>goal</code> <code>dict[str, Any] | Goal</code> <p>The goal to achieve</p> required <code>max_depth</code> <code>int | None</code> <p>Optional maximum depth for the search</p> <code>None</code> <code>heuristic_fn</code> <code>HeuristicFn | None</code> <p>Optional custom heuristic function for this plan</p> <code>None</code> <code>time_budget</code> <code>float | None</code> <p>Optional wall-clock budget in seconds for the search. Behaves exactly as in :meth:<code>generate_plan</code>.</p> <code>None</code> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the inputs have invalid types.</p> <code>ValueError</code> <p>If the inputs are invalid.</p> <code>Exception</code> <p>Any error from predicates, providers, or hooks propagates -- planning fails loudly instead of returning an empty result.</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.begin_execution","title":"<code>begin_execution(initial_state, plan, goal=None)</code>","text":"<p>Begin an interruptible execution of a plan, returning a handle.</p> <p>The handle (:class:<code>goapauto.models.execution.PlanExecution</code>) lets callers interrupt the in-flight run between actions via <code>execution.interrupt(reason, source=...)</code> -- from another thread, a hook, or a watchdog. Run it with <code>execution.run()</code> / <code>execution.arun()</code>; an honored interrupt raises :class:<code>goapauto.models.execution.PlanInterruptedError</code> carrying the partial state, executed/remaining actions, and the reason.</p> <p>Container/step type validation happens eagerly here (fail fast); action-name resolution stays lazy per step because providers are state-dependent. Steps are normalized to action-name strings at <code>begin_execution</code> time. The deep copy of <code>initial_state</code> happens at <code>run()</code>/<code>arun()</code> start, not here -- parity with <code>execute_plan</code> (same TOCTOU contract).</p> <p>Parameters:</p> Name Type Description Default <code>initial_state</code> <code>WorldState</code> <p>Starting WorldState</p> required <code>plan</code> <code>Plan | list[Action] | PlanResult</code> <p>Sequence of action names, Action objects, or a PlanResult</p> required <code>goal</code> <code>Goal | None</code> <p>Optional Goal context for dynamic action providers</p> <code>None</code> <p>Returns:</p> Type Description <code>PlanExecution</code> <p>A PlanExecution handle for the normalized plan</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If initial_state/plan has an invalid type</p> <code>ValueError</code> <p>If PlanResult.plan is None</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.continue_plan","title":"<code>continue_plan(world_state, goal, executed_actions, max_depth=None, heuristic_fn=None, time_budget=None)</code>","text":"<p>Continue planning from a checkpoint after executing some actions.</p> <p>This allows incremental replanning by reusing the already-executed actions and finding the remainder of the plan from the current state.</p> <p>Parameters:</p> Name Type Description Default <code>world_state</code> <code>dict[str, Any] | WorldState</code> <p>The current state of the world (after executing actions)</p> required <code>goal</code> <code>dict[str, Any] | Goal</code> <p>The goal to achieve</p> required <code>executed_actions</code> <code>list[str]</code> <p>List of action names that have already been executed</p> required <code>max_depth</code> <code>int | None</code> <p>Optional maximum depth for the search</p> <code>None</code> <code>heuristic_fn</code> <code>HeuristicFn | None</code> <p>Optional custom heuristic function for this plan</p> <code>None</code> <code>time_budget</code> <code>float | None</code> <p>Optional wall-clock budget in seconds for the search. Behaves exactly as in :meth:<code>generate_plan</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>PlanResult</code> <p>PlanResult with the remaining plan steps</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the inputs have invalid types.</p> <code>ValueError</code> <p>If the inputs are invalid.</p> <code>Exception</code> <p>Any error from predicates, providers, or hooks propagates -- planning fails loudly instead of returning an empty result.</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.execute_plan","title":"<code>execute_plan(initial_state, plan, goal=None)</code>","text":"<p>Execute a sequence of actions on the initial state and return the final state.</p> <p>Parameters:</p> Name Type Description Default <code>initial_state</code> <code>WorldState</code> <p>Starting WorldState</p> required <code>plan</code> <code>Plan | list[Action] | PlanResult</code> <p>Sequence of action names, Action objects, or a PlanResult</p> required <code>goal</code> <code>Goal | None</code> <p>Optional Goal context for dynamic action providers</p> <code>None</code> <p>Returns:</p> Type Description <code>WorldState</code> <p>WorldState after executing all plan steps</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If initial_state/plan has an invalid type or an async handler is used</p> <code>ValueError</code> <p>If PlanResult.plan is None</p> <code>KeyError</code> <p>If an action name is not found in registered providers</p> <code>PlanExecutionError</code> <p>If an action's preconditions are not met</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.generate_plan","title":"<code>generate_plan(world_state, goal, max_depth=None, heuristic_fn=None, time_budget=None)</code>","text":"<p>Generate a plan to achieve the given goal.</p> <p>Parameters:</p> Name Type Description Default <code>world_state</code> <code>dict[str, Any] | WorldState</code> <p>The current state of the world</p> required <code>goal</code> <code>dict[str, Any] | Goal</code> <p>The goal to achieve</p> required <code>max_depth</code> <code>int | None</code> <p>Optional maximum depth for the search</p> <code>None</code> <code>heuristic_fn</code> <code>HeuristicFn | None</code> <p>Optional custom heuristic function for this plan. When omitted the search runs Dijkstra's algorithm (h=0).</p> <code>None</code> <code>time_budget</code> <code>float | None</code> <p>Optional wall-clock budget in seconds for the search. When the deadline passes the search stops and the result reports the miss explicitly (<code>plan</code> is None, <code>stats.budget_exhausted</code> is True) instead of hanging the tick. <code>None</code> (default) means unlimited -- the search behaves exactly as before.</p> <code>None</code> <p>Returns:</p> Type Description <code>PlanResult</code> <p>PlanResult. <code>plan</code> is None when no plan was found.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the inputs have invalid types.</p> <code>ValueError</code> <p>If the inputs are invalid.</p> <code>Exception</code> <p>Any error from predicates, providers, or hooks propagates -- planning fails loudly instead of returning an empty result.</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.get_action","title":"<code>get_action(name, state, *, goal=None)</code>","text":"<p>Look up an action by name across all registered ActionProviders.</p> <p>Public form of the provider lookup the planner uses internally. Re-queries providers on every call, so dynamic (e.g. model-backed) providers are always fresh -- with expensive providers that is a provider call per invocation.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The action name to look up</p> required <code>state</code> <code>WorldState</code> <p>The WorldState context for dynamic providers</p> required <code>goal</code> <code>Goal | None</code> <p>Optional Goal context for dynamic providers</p> <code>None</code> <p>Returns:</p> Type Description <code>Action | None</code> <p>The matching Action, or None if no provider offers that name.</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.get_search_graph","title":"<code>get_search_graph()</code>","text":"<p>Return the search graph from the last planning operation.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary containing nodes and edges of the search graph:</p> <code>dict[str, Any]</code> <p>{ \"nodes\": {node_id: {id, state, g, h, f, parent, action}}, \"edges\": [{\"from\", \"to\", \"action\", \"cost\"}], \"metadata\": {expanded_count, visited_count, max_depth_reached}</p> <code>dict[str, Any]</code> <p>}</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.register_execution_handler","title":"<code>register_execution_handler(action_name, handler)</code>","text":"<p>Register a custom execution handler for an action.</p> <p>Parameters:</p> Name Type Description Default <code>action_name</code> <code>str</code> <p>The name of the action to handle</p> required <code>handler</code> <code>Callable[..., Any]</code> <p>Callable that takes (current_state, action) and returns updated WorldState</p> required"},{"location":"reference/planner/#goapauto.models.goap_planner.Planner.register_hook","title":"<code>register_hook(event, callback)</code>","text":"<p>Register a callback for a specific planner event.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>str</code> <p>One of 'on_node_expanded', 'on_plan_found', 'on_search_failed', 'on_budget_exhausted', 'on_action_start', 'on_action_complete', 'on_action_failed', 'on_execution_complete', 'on_execution_failed'</p> required <code>callback</code> <code>Callable[..., Any]</code> <p>The function to call when the event occurs</p> required"},{"location":"reference/planner/#planexecutionerror","title":"PlanExecutionError","text":"<p>Exception raised when plan execution fails (e.g., action precondition not met).</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.PlanExecutionError","title":"<code>goapauto.models.goap_planner.PlanExecutionError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Exception raised when plan execution fails (e.g. precondition not met).</p>"},{"location":"reference/planner/#planresult","title":"PlanResult","text":"<p>The result of a planning operation, returned by <code>generate_plan</code>.</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.PlanResult","title":"<code>goapauto.models.goap_planner.PlanResult</code>","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Result of a planning operation.</p>"},{"location":"reference/planner/#planstats","title":"PlanStats","text":"<p>Statistics about the last planning process.</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.PlanStats","title":"<code>goapauto.models.goap_planner.PlanStats</code>  <code>dataclass</code>","text":"<p>Statistics about the planning process.</p>"},{"location":"reference/planner/#schedule-schedulestep","title":"Schedule &amp; ScheduleStep","text":"<p>Temporal planning types: a schedule of steps with start/end times and makespan.</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Schedule","title":"<code>goapauto.models.goap_planner.Schedule</code>  <code>dataclass</code>","text":"<p>Temporal schedule for a plan with action start/end times.</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.Schedule.to_list","title":"<code>to_list()</code>","text":"<p>Convert to list of dicts for serialization.</p>"},{"location":"reference/planner/#goapauto.models.goap_planner.ScheduleStep","title":"<code>goapauto.models.goap_planner.ScheduleStep</code>  <code>dataclass</code>","text":"<p>A single step in a temporal schedule.</p>"},{"location":"reference/replan/","title":"Replan API Reference","text":"<p>The <code>ReplanPolicy</code> class owns all replan bookkeeping: it detects when the current plan or goal is stale and replans with time-budgeted search. It is the only consumer-facing wrapper of the wave-1 runtime-budget seam (<code>Planner(time_budget=...)</code>).</p> <pre><code>from goapauto import ReplanPolicy, ReplanReason\n\npolicy = ReplanPolicy(\n    planner,\n    watched_keys=frozenset({\"enemy_visible\", \"door_open\"}),\n    min_replan_interval=0.5,\n    max_replans_per_tick=2,\n)\npolicy.adopt(world_state, goal)\n\ndecision = policy.should_replan(world_state, goal, plan, tick_id=7)\nif decision.replan:\n    result = policy.replan(world_state, goal, decision, time_budget=0.1)\n</code></pre>"},{"location":"reference/replan/#replanpolicy","title":"ReplanPolicy","text":""},{"location":"reference/replan/#goapauto.models.replan.ReplanPolicy","title":"<code>goapauto.models.replan.ReplanPolicy</code>","text":"<p>Decides when to replan and replans through the wrapped planner.</p> <p>The host drives the loop: <code>adopt()</code> once (or let <code>replan()</code> adopt implicitly), <code>should_replan()</code> between actions, <code>replan()</code> on a replan decision, <code>note_executed()</code> after each executed action.</p> <p>Everything is opt-in; the defaults only install the two structural triggers (invalid next action, changed goal). Not thread-safe, like the planner it wraps.</p>"},{"location":"reference/replan/#goapauto.models.replan.ReplanPolicy.__init__","title":"<code>__init__(planner, min_replan_interval=0.0, replan_on_invalid=True, replan_on_goal_change=True, watched_keys=None, max_sensor_age=None, time_budget=None, max_replans_per_tick=None, max_consecutive_unthrottled_replans=None, reuse_prefix_on_invalid=True, clock=time.monotonic)</code>","text":"<p>Create a policy around an existing planner.</p> <p>Parameters:</p> Name Type Description Default <code>planner</code> <code>Planner</code> <p>The planner replans are executed through.</p> required <code>min_replan_interval</code> <code>float</code> <p>Seconds between optional (world-change) replans; <code>0.0</code> disables throttling.</p> <code>0.0</code> <code>replan_on_invalid</code> <code>bool</code> <p>Replan when the next action's preconditions fail (bypasses the interval: a broken plan cannot execute).</p> <code>True</code> <code>replan_on_goal_change</code> <code>bool</code> <p>Replan when the selected goal differs from the planned-for goal (also bypasses the interval).</p> <code>True</code> <code>watched_keys</code> <code>frozenset[str] | None</code> <p>World-state keys whose change triggers a replan; <code>None</code> disables the world-change trigger.</p> <code>None</code> <code>max_sensor_age</code> <code>float | None</code> <p>When set, a world-change replan is suppressed while <code>sensor_age</code> exceeds it (<code>SENSORS_STALE</code>); structural triggers still fire.</p> <code>None</code> <code>time_budget</code> <code>float | None</code> <p>Passed through to the planner calls <code>replan()</code> makes; validated eagerly, exactly like the planner's own.</p> <code>None</code> <code>max_replans_per_tick</code> <code>int | None</code> <p>Within-tick replan cap; requires the host to pass <code>tick_id</code> to <code>should_replan</code>. <code>None</code> disables.</p> <code>None</code> <code>max_consecutive_unthrottled_replans</code> <code>int | None</code> <p>Circuit breaker -- after this many consecutive unthrottled replans the circuit opens and further unthrottled triggers are answered <code>THROTTLED</code> until steady state, <code>adopt()</code>, <code>reset()</code>, or a successful world-change probe replan closes it. <code>None</code> disables.</p> <code>None</code> <code>reuse_prefix_on_invalid</code> <code>bool</code> <p>Route invalid/world-changed replans through <code>continue_plan</code> (prefix reuse); <code>False</code> routes them through a fresh <code>generate_plan</code> (for cyclic/toggle domains where order-based prefix stripping can drop a needed step).</p> <code>True</code> <code>clock</code> <code>Callable[[], float]</code> <p>Injectable clock (seconds); <code>time.monotonic</code> default.</p> <code>monotonic</code>"},{"location":"reference/replan/#goapauto.models.replan.ReplanPolicy.adopt","title":"<code>adopt(state, goal)</code>","text":"<p>Adopt an externally-created plan as the policy's baseline.</p> <p>Seeds the state snapshot, the planned-for goal, and the plan clock; clears the executed prefix and resets the streak/circuit/tick counters. Required before the first :meth:<code>should_replan</code> -- without it the policy would diff against nothing and hallucinate a goal change. <code>replan()</code> also adopts implicitly after planning.</p>"},{"location":"reference/replan/#goapauto.models.replan.ReplanPolicy.note_executed","title":"<code>note_executed(action_name)</code>","text":"<p>Record an executed action name for prefix reuse on replan.</p> <p>The policy never executes anything itself; the host calls this after each executed action (or bridges design 03's <code>PlanInterruptedError.executed_actions</code> into it).</p>"},{"location":"reference/replan/#goapauto.models.replan.ReplanPolicy.register_hook","title":"<code>register_hook(event, callback)</code>","text":"<p>Register a callback for a policy event.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>str</code> <p>One of 'on_replan', 'on_replan_skipped'</p> required <code>callback</code> <code>Callable[..., Any]</code> <p>The function to call when the event occurs</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the event name is unknown.</p>"},{"location":"reference/replan/#goapauto.models.replan.ReplanPolicy.replan","title":"<code>replan(state, goal, decision, tick_id=None)</code>","text":"<p>Act on a replan decision through the wrapped planner.</p> <p>Re-checks nothing about the world; it validates the call, plans, and adopts the outcome implicitly. <code>PLAN_INVALID</code> / <code>WORLD_CHANGED</code> route through <code>continue_plan</code> (reusing the executed prefix) unless <code>reuse_prefix_on_invalid</code> is <code>False</code>; <code>GOAL_CHANGED</code> / <code>NO_PLAN</code> take a fresh <code>generate_plan</code>.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>WorldState</code> <p>The state the decision was computed from (identity checked -- pass the same objects).</p> required <code>goal</code> <code>Goal</code> <p>The goal the decision was computed from (identity checked).</p> required <code>decision</code> <code>ReplanDecision</code> <p>A decision with <code>replan=True</code> from :meth:<code>should_replan</code>.</p> required <code>tick_id</code> <code>int | str | None</code> <p>Opaque tick context; defaults to the decision's <code>tick_id</code> when present.</p> <code>None</code> <p>Returns:</p> Type Description <code>PlanResult</code> <p>The fresh <code>PlanResult</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the decision says not to replan, carries an unexpected reason, or was computed from different state/goal objects.</p>"},{"location":"reference/replan/#goapauto.models.replan.ReplanPolicy.reset","title":"<code>reset()</code>","text":"<p>Forget everything, including adoption.</p> <p>After <code>reset()</code> the policy must be adopted again before :meth:<code>should_replan</code> will answer.</p>"},{"location":"reference/replan/#goapauto.models.replan.ReplanPolicy.should_replan","title":"<code>should_replan(state, goal, plan, next_index=0, *, sensor_age=None, was_interrupted=False, tick_id=None, emit_hooks=True)</code>","text":"<p>Decide whether to replan, in a fixed evaluation order.</p> <p>Order: goal satisfied -&gt; no plan -&gt; next-action validity (or forced by <code>was_interrupted</code>) -&gt; goal changed -&gt; staleness gate -&gt; world change (throttled) -&gt; plan valid. A would-be replan is then suppressed by the circuit breaker, then the per-tick cap.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>WorldState</code> <p>Current world state.</p> required <code>goal</code> <code>Goal</code> <p>Currently selected goal.</p> required <code>plan</code> <code>Plan | PlanResult | None</code> <p>The plan under execution -- a <code>Plan</code> list, the <code>PlanResult</code> it came from (unwrapped), or <code>None</code>.</p> required <code>next_index</code> <code>int</code> <p>Index of the next action to execute.</p> <code>0</code> <code>sensor_age</code> <code>float | None</code> <p>Seconds since last good sensor data; <code>None</code> means unknown/fresh. Only consulted with the world-change trigger when <code>max_sensor_age</code> is set.</p> <code>None</code> <code>was_interrupted</code> <code>bool</code> <p>Design-03 interruption landed here -- forces <code>PLAN_INVALID</code> regardless of applicability.</p> <code>False</code> <code>tick_id</code> <code>int | str | None</code> <p>Opaque tick context, passed through into <code>detail</code> and hooks; required when <code>max_replans_per_tick</code> is set.</p> <code>None</code> <code>emit_hooks</code> <code>bool</code> <p><code>False</code> decides without firing <code>on_replan_skipped</code> (side-effect-free preview).</p> <code>True</code> <p>Returns:</p> Type Description <code>ReplanDecision</code> <p>A frozen :class:<code>ReplanDecision</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the policy has no adopted plan, <code>next_index</code> is negative, or a per-tick cap is set without <code>tick_id</code>.</p> <code>TypeError</code> <p>If <code>state</code>/<code>goal</code>/<code>plan</code> have invalid types.</p>"},{"location":"reference/replan/#replandecision","title":"ReplanDecision","text":""},{"location":"reference/replan/#goapauto.models.replan.ReplanDecision","title":"<code>goapauto.models.replan.ReplanDecision</code>  <code>dataclass</code>","text":"<p>Outcome of :meth:<code>ReplanPolicy.should_replan</code>.</p> <p>Frozen for value semantics but not hashable (<code>detail</code> is a dict): equality works, <code>hash()</code> does not. <code>detail</code> values are restricted to JSON-safe scalars (<code>str</code>, <code>int</code>, <code>float</code>, <code>bool</code>, <code>None</code>, and lists thereof) -- the diagnostics layer serializes decisions, so the offending action is recorded as its name string, never the :class:<code>Action</code> object.</p>"},{"location":"reference/replan/#replanreason","title":"ReplanReason","text":""},{"location":"reference/replan/#goapauto.models.replan.ReplanReason","title":"<code>goapauto.models.replan.ReplanReason</code>","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Why a replan decision came out the way it did.</p> <p>A <code>str</code> enum: the wire form is the plain string value, which is also what the diagnostics layer's string-typed fields consume (<code>decision.reason.value</code>).</p>"},{"location":"reference/visualizer/","title":"Visualizer API Reference","text":"<p>The <code>SearchTreeVisualizer</code> hooks into the planner to visualize the search tree.</p> <pre><code>from goapauto.utils.visualizer import SearchTreeVisualizer\n\nviz = SearchTreeVisualizer()\nplanner.register_hook(\"on_node_expanded\", viz.on_node_expanded)\n# ... plan ...\nviz.export(\"tree.mmd\")\n</code></pre>"},{"location":"reference/visualizer/#goapauto.utils.visualizer.SearchTreeVisualizer","title":"<code>goapauto.utils.visualizer.SearchTreeVisualizer</code>","text":"<p>Utility for capturing and exporting the A* search tree.</p> <p>This visualizer hooks into the Planner's 'on_node_expanded' event to build a representation of the search space that can be exported to various formats.</p>"},{"location":"reference/visualizer/#goapauto.utils.visualizer.SearchTreeVisualizer.clear","title":"<code>clear()</code>","text":"<p>Reset the captured data.</p>"},{"location":"reference/visualizer/#goapauto.utils.visualizer.SearchTreeVisualizer.export","title":"<code>export(filepath)</code>","text":"<p>Export the search tree to a file.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to save the file. If extension is .md, wraps in mermaid blocks.       Defaulting to mermaid content for other extensions.</p> required"},{"location":"reference/visualizer/#goapauto.utils.visualizer.SearchTreeVisualizer.on_node_expanded","title":"<code>on_node_expanded(node)</code>","text":"<p>Capture a node and its connection to its parent.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Node</code> <p>The node being expanded</p> required"},{"location":"reference/visualizer/#goapauto.utils.visualizer.SearchTreeVisualizer.to_graphviz","title":"<code>to_graphviz()</code>","text":"<p>Generate a Graphviz DOT string for the search tree.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string in DOT format</p>"},{"location":"reference/visualizer/#goapauto.utils.visualizer.SearchTreeVisualizer.to_mermaid","title":"<code>to_mermaid()</code>","text":"<p>Generate a Mermaid diagram string for the search tree.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string formatted as a Mermaid flowchart (graph TD)</p>"},{"location":"reference/visualizer/#search-graph-export","title":"Search Graph Export","text":"<p>The planner also provides direct access to the search graph data:</p> <pre><code>result = planner.generate_plan(state, goal)\ngraph = planner.get_search_graph()\n\n# graph structure:\n{\n    \"nodes\": {\n        node_id: {\n            \"id\": int,\n            \"state\": dict,\n            \"g\": float,      # cost from start\n            \"h\": float,      # heuristic\n            \"f\": float,      # total\n            \"parent\": int,\n            \"action\": str\n        }\n    },\n    \"edges\": [\n        {\"from\": int, \"to\": int, \"action\": str, \"cost\": float}\n    ],\n    \"metadata\": {\n        \"expanded_count\": int,\n        \"visited_count\": int,\n        \"max_depth_reached\": int\n    }\n}\n</code></pre>"},{"location":"reference/worldstate/","title":"WorldState API Reference","text":"<p>The <code>WorldState</code> class represents the world state with attribute-style access. It inherits from <code>pydantic.BaseModel</code>.</p> <pre><code>from goapauto.models.worldstate import WorldState\n\n# Initialization (Keyword-only arguments)\nstate = WorldState(has_wood=True, count=5)\n# dictionary access\nval = state['has_wood']\n# attribute access\nval = state.has_wood\n</code></pre>"},{"location":"reference/worldstate/#distinguishing-unknown-from-falsenone","title":"Distinguishing Unknown from False/None","text":"<p><code>WorldState</code> distinguishes between \"never set\" and \"explicitly set to False/None\":</p> <pre><code>from goapauto.models.worldstate import _UNKNOWN\n\nstate = WorldState()\nstate.get(\"x\")                    # returns _UNKNOWN (never set)\nstate.is_known(\"x\")               # False\n\nstate.x = False\nstate.is_known(\"x\")               # True (explicitly set to False)\nstate.get(\"x\")                    # False\n\nstate.y = None\nstate.is_known(\"y\")               # True (explicitly set to None)\nstate.get(\"y\")                    # None\n\ndel state.x\nstate.is_known(\"x\")               # False (deleted)\nstate.get(\"x\")                    # returns _UNKNOWN\n</code></pre> <p>Use <code>is_known()</code> in sensors/logic to distinguish \"never sensed\" from \"sensed as false\".</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState","title":"<code>goapauto.models.worldstate.WorldState</code>","text":"<p>A class representing the world state with attribute-style access.</p> <p>This class uses Pydantic for validation and type safety. It provides a dictionary-like interface and attribute-style access to state values.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.__bool__","title":"<code>__bool__()</code>","text":"<p>Check if the state is non-empty.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.__contains__","title":"<code>__contains__(key)</code>","text":"<p>Check if a state key exists.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.__getitem__","title":"<code>__getitem__(key)</code>","text":"<p>Get a state value using dictionary access.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.__hash__","title":"<code>__hash__()</code>","text":"<p>Compute a hash value for this state.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.__iter__","title":"<code>__iter__()</code>","text":"<p>Iterate over state keys.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.__len__","title":"<code>__len__()</code>","text":"<p>Get the number of state values.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.__setattr__","title":"<code>__setattr__(name, value)</code>","text":"<p>Set an attribute, rejecting unhashable values up front.</p> <p>Private/dunder names (pydantic internals) skip the check; every real state key must stay hashable so the planner can hash states.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.__setitem__","title":"<code>__setitem__(key, value)</code>","text":"<p>Set a state value using dictionary access.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.clear","title":"<code>clear()</code>","text":"<p>Clear all state values.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.copy","title":"<code>copy(deep=False)</code>","text":"<p>Create a copy of this WorldState.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.diff","title":"<code>diff(other)</code>","text":"<p>Get the differences between this state and another.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.from_dict","title":"<code>from_dict(data)</code>  <code>classmethod</code>","text":"<p>Create a new WorldState from a dictionary.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.get","title":"<code>get(key, default=_UNKNOWN)</code>","text":"<p>Get a state value with a default if it doesn't exist.</p> <p>If default is not provided and key is not set, returns _UNKNOWN sentinel which allows distinguishing between \"not set\" and \"explicitly set to None/False\".</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.get_state","title":"<code>get_state()</code>","text":"<p>Get a copy of the current state as a dictionary.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.is_known","title":"<code>is_known(key)</code>","text":"<p>Check if an attribute has been explicitly set in the state.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the attribute was explicitly set (even to None/False),</p> <code>bool</code> <p>False if the attribute was never set.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.items","title":"<code>items()</code>","text":"<p>Return a view of (key, value) pairs.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.keys","title":"<code>keys()</code>","text":"<p>Return a view of state keys.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.to_dict","title":"<code>to_dict()</code>","text":"<p>Convert the state to a dictionary.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.update","title":"<code>update(other, **kwargs)</code>","text":"<p>Update the state with values from a dictionary or another WorldState.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.update_state","title":"<code>update_state(updates)</code>","text":"<p>Update multiple state values at once.</p>"},{"location":"reference/worldstate/#goapauto.models.worldstate.WorldState.values","title":"<code>values()</code>","text":"<p>Return a view of state values.</p>"},{"location":"user_guide/core_concepts/","title":"Core Concepts &amp; Architecture","text":"<p><code>goapauto</code> implements a standard Goal-Oriented Action Planning architecture modularized for Python.</p>"},{"location":"user_guide/core_concepts/#system-components","title":"System Components","text":""},{"location":"user_guide/core_concepts/#1-world-model-worldstate","title":"1. World Model (<code>WorldState</code>)","text":"<ul> <li>Responsibility: Stores the current state of the environment and agent.</li> <li>Design: Pydantic model with dynamic attribute access (<code>state.key</code> or <code>state['key']</code>).</li> <li>Mutability: Methods like <code>update()</code> mutate the instance; <code>copy()</code> and <code>apply()</code> create new instances.</li> <li>Three-valued logic: Distinguishes \"unknown\" (never set) from \"explicitly False/None\" via <code>_UNKNOWN</code> sentinel and <code>is_known()</code> method.</li> </ul>"},{"location":"user_guide/core_concepts/#2-action-system","title":"2. Action System","text":"<ul> <li>Action: Atomic unit containing:<ul> <li><code>preconditions</code>: Predicates (<code>Equal</code>, <code>GreaterThan</code>, <code>LessThan</code>, <code>Range</code>, callables) that must match the world state.</li> <li><code>effects</code>: Transformations (<code>Set</code>, <code>Increment</code>, <code>Decrement</code>, <code>Unset</code>/<code>Delete</code>) applied to the state.</li> <li><code>cost</code>: Scalar float or multi-dimensional dict/list for optimization.</li> <li><code>duration</code>: Optional float for temporal planning (seconds).</li> </ul> </li> <li>Planner: Uses A* search to find the lowest-cost sequence of actions connecting <code>Start State</code> -&gt; <code>Goal State</code>.</li> <li>Cost optimization: <code>Planner(cost_weights=...)</code> supports weighted multi-dimensional costs.</li> <li>Temporal planning: Actions with <code>duration</code> generate a <code>Schedule</code> with start/end times and makespan.</li> </ul>"},{"location":"user_guide/core_concepts/#3-perception-arbitration-the-brain-loop","title":"3. Perception &amp; Arbitration (The \"Brain\" Loop)","text":"<p>For continuous agents, the system operates in a loop:</p> <ol> <li>Sense: <code>SensorManager</code> aggregates data from <code>Sensor</code>s to update <code>WorldState</code>.</li> <li>Think (Arbitrate): <code>GoalArbitrator</code> evaluates all <code>Goal</code>s and selects the highest-priority one that is not yet satisfied.</li> <li>Plan: <code>Planner</code> generates a specific plan to satisfy the selected <code>Goal</code>.</li> <li>Act: The agent executes the plan (actions).</li> </ol>"},{"location":"user_guide/core_concepts/#4-goal-system","title":"4. Goal System","text":"<ul> <li>Goal: Target state with priority and optional name.</li> <li>Target values: Support exact matches, predicates (<code>GreaterThan</code>, <code>LessThan</code>, <code>Range</code>), and callables (lambdas).</li> <li>Priority: Lower number = higher priority.</li> <li>Context-aware actions: Action providers receive the current goal to generate context-aware actions.</li> </ul>"},{"location":"user_guide/core_concepts/#5-planning-search","title":"5. Planning &amp; Search","text":"<ul> <li>Algorithm: A* search with configurable heuristic (<code>Node.heuristic</code> or <code>Node.numeric_heuristic</code>).</li> <li>Depth limiting: <code>max_depth</code> parameter to limit search depth.</li> <li>Hooks: <code>on_node_expanded</code>, <code>on_plan_found</code>, <code>on_search_failed</code> for observability.</li> <li>Search graph: <code>Planner.get_search_graph()</code> exposes nodes, edges, and metadata for visualization/analysis.</li> <li>Incremental replanning: <code>continue_plan()</code> resumes from a checkpoint with already-executed actions.</li> </ul>"},{"location":"user_guide/core_concepts/#6-temporal-multi-dimensional-planning","title":"6. Temporal &amp; Multi-dimensional Planning","text":"<ul> <li>Duration: Actions can have <code>duration</code> for temporal planning; planner builds sequential <code>Schedule</code>.</li> <li>Multi-dimensional costs: Action costs can be dict/list; <code>cost_weights</code> in Planner defines optimization weights.</li> <li>Numeric heuristic: <code>Node.numeric_heuristic</code> provides distance-based heuristic for numeric goals.</li> <li>Schedules: <code>PlanResult.schedule</code> contains <code>ScheduleStep</code> with start/end times, duration, and makespan.</li> </ul>"},{"location":"user_guide/core_concepts/#7-state-representation","title":"7. State Representation","text":"<ul> <li>Three-valued logic: <code>WorldState</code> distinguishes \"unknown\" (never set, returns <code>_UNKNOWN</code>) from explicit <code>False</code>/<code>None</code> via <code>is_known()</code>.</li> <li>Effect types: <code>Set</code>, <code>Increment</code>, <code>Decrement</code>, <code>Unset</code>/<code>Delete</code> applied via <code>update_state()</code>.</li> <li>Immutability: <code>apply()</code> and <code>copy()</code> create new states; <code>update()</code> mutates in place.</li> </ul>"},{"location":"user_guide/core_concepts/#data-flow-diagram","title":"Data Flow Diagram","text":"<pre><code>graph TD\n    Env[Environment] --&gt;|Sense| Sensors\n    Sensors --&gt;|Update| State[WorldState]\n    State --&gt;|Input| Arbitrator\n    Goals[Goal List] --&gt;|Input| Arbitrator\n    Arbitrator --&gt;|Selected Goal| Planner\n    Actions[Action Set] --&gt;|Available Actions| Planner\n    Planning[A* Search] --&gt;|Plan + Schedule| Execution\n    Execution --&gt;|Apply| Env\n</code></pre>"},{"location":"user_guide/core_concepts/#design-decisions","title":"Design Decisions","text":"<ul> <li>Loose Coupling: Sensors and Arbitrators are optional. You can use the <code>Planner</code> purely as a pathfinding utility.</li> <li>Strict Typing: All core models enforce types to catch configuration errors early.</li> <li>Observability: Hooks, Visualization, and Search Graph allow introspection into the planning process (White-box AI).</li> <li>Flexible Cost Model: Supports scalar, dict, and list costs with configurable weights.</li> <li>Temporal Awareness: Optional duration and schedule generation for real-time systems.</li> </ul>"},{"location":"user_guide/getting_started/","title":"User Guide: Getting Started","text":"<p>This guide explains how to use <code>goapauto</code> to build intelligent agents.</p>"},{"location":"user_guide/getting_started/#1-define-the-world-state","title":"1. Define the World State","text":"<p>The <code>WorldState</code> is the agent's memory or belief about the world. It is initialized with keyword arguments.</p> <pre><code>from goapauto.models.worldstate import WorldState\n\n# Define initial state\nstate = WorldState(\n    hungry=True,\n    has_food=False,\n    location=\"bedroom\"\n)\n</code></pre>"},{"location":"user_guide/getting_started/#distinguishing-unknown-from-false","title":"Distinguishing Unknown from False","text":"<p><code>WorldState</code> distinguishes \"never set\" from \"explicitly False/None\":</p> <pre><code>from goapauto.models.worldstate import _UNKNOWN\n\nstate = WorldState()\nprint(state.get(\"x\"))           # _UNKNOWN (never set)\nprint(state.is_known(\"x\"))      # False\n\nstate.x = False\nprint(state.get(\"x\"))           # False\nprint(state.is_known(\"x\"))      # True\n\ndel state.x\nprint(state.get(\"x\"))           # _UNKNOWN (deleted)\n</code></pre>"},{"location":"user_guide/getting_started/#2-define-actions","title":"2. Define Actions","text":"<p>Actions transform the state. They have preconditions (requirements) and effects (changes).</p> <pre><code>from goapauto.models.actions import Action, Set, Increment, Range\n\n# Action: specific definition\neat_action = Action(\n    name=\"eat_food\",\n    preconditions={\"has_food\": True},\n    effects={\n        \"hungry\": False, \n        \"has_food\": False\n    },\n    cost=1.0\n)\n\n# Action: using reusable Effects\nmove_to_kitchen = Action(\n    name=\"goto_kitchen\",\n    preconditions={\"location\": \"bedroom\"},\n    effects={\"location\": Set(\"kitchen\")},\n    cost=2.0\n)\n\n# Temporal planning with duration\nbuild_action = Action(\n    name=\"build_shelter\",\n    preconditions={\"has_wood\": True},\n    effects={\"has_shelter\": True},\n    cost=5.0,\n    duration=30.0  # seconds\n)\n\n# Multi-dimensional costs\nnavigate = Action(\n    name=\"navigate\",\n    preconditions={},\n    effects={\"location\": Set(\"target\")},\n    cost={\"time\": 30.0, \"energy\": 5.0, \"risk\": 2.0},\n    duration=30.0\n)\n\n# Range preconditions\nrest_action = Action(\n    name=\"rest\",\n    preconditions={\"energy\": Range(0, 30)},  # only if energy &lt; 30\n    effects={\"energy\": 100},\n    cost=1.0,\n    duration=10.0\n)\n</code></pre>"},{"location":"user_guide/getting_started/#3-define-goals","title":"3. Define Goals","text":"<p>A <code>Goal</code> describes a target state the agent wants to reach.</p> <pre><code>from goapauto.models.goal import Goal\nfrom goapauto.models.actions import GreaterThan, LessThan, Range\n\n# Exact value goal\ngoal = Goal(\n    target_state={\"hungry\": False},\n    priority=1\n)\n\n# Inequality goals\nsurvival_goal = Goal(\n    target_state={\n        \"health\": GreaterThan(50),        # health &gt; 50\n        \"ammo\": Range(10, 100),           # 10 &lt;= ammo &lt;= 100\n        \"thirst\": LessThan(50)            # thirst &lt; 50\n    },\n    priority=1\n)\n\n# Callable targets (custom logic)\ncustom_goal = Goal(\n    target_state={\n        \"enemy_distance\": lambda v: v &gt; 10,  # distance &gt; 10\n        \"has_weapon\": lambda v: v is True\n    }\n)\n</code></pre>"},{"location":"user_guide/getting_started/#4-run-the-planner","title":"4. Run the Planner","text":"<p>Feed the actions into the <code>Planner</code> and request a plan to the goal.</p> <pre><code>from goapauto.models.goap_planner import Planner\n\n# Basic planner\nplanner = Planner(actions_list=[\n    eat_action,\n    move_to_kitchen,\n    build_action\n])\n\nresult = planner.generate_plan(state, goal)\n\nif result.plan:\n    print(\"Plan found:\", result.plan)\n    # Output: ['goto_kitchen', 'eat_food']\nelse:\n    print(\"No plan found:\", result.message)\n</code></pre>"},{"location":"user_guide/getting_started/#planner-configuration","title":"Planner Configuration","text":"<pre><code># Verbose output (default True)\nplanner = Planner(actions_list=actions, verbose=False)\n\n# Custom logger\nimport logging\nlogger = logging.getLogger(\"my_planner\")\nplanner = Planner(actions_list=actions, logger=logger)\n\n# Multi-dimensional cost optimization\nplanner = Planner(\n    actions_list=actions,\n    cost_weights={\"time\": 1.0, \"energy\": 0.5, \"risk\": 2.0}\n)\n\n# Custom heuristic for numeric goals\nfrom goapauto.models.node import Node\nplanner = Planner(actions_list=actions, heuristic_fn=Node.numeric_heuristic)\n\n# Limit search depth\nresult = planner.generate_plan(state, goal, max_depth=10)\n</code></pre>"},{"location":"user_guide/getting_started/#planresult","title":"PlanResult","text":"<pre><code>result = planner.generate_plan(state, goal)\n\n# Access plan\nprint(result.plan)          # ['action1', 'action2']\nprint(result.message)       # success/error message\nprint(result.total_cost)    # total cost (float)\n\n# Temporal schedule (if actions have duration)\nif result.schedule:\n    print(\"Makespan:\", result.schedule.makespan)\n    for step in result.schedule.steps:\n        print(f\"  {step.action}: {step.start_time}-{step.end_time}\")\n\n# Search graph for debugging/visualization\ngraph = planner.get_search_graph()\nprint(f\"Nodes: {len(graph['nodes'])}, Edges: {len(graph['edges'])}\")\n</code></pre>"},{"location":"user_guide/getting_started/#5-advanced-features","title":"5. Advanced Features","text":""},{"location":"user_guide/getting_started/#incremental-replanning","title":"Incremental Replanning","text":"<pre><code># Execute part of plan\nstate = WorldState(x=0)\nresult = planner.generate_plan(state, {\"x\": 5})\n# ['inc', 'inc', 'inc', 'inc', 'inc']\n\n# Execute first action\nstate2 = planner.execute_plan(state, result.plan[:1])  # x=1\n\n# Continue from current state\nremaining = planner.continue_plan(state2, {\"x\": 5}, executed_actions=[\"inc\"])\nprint(remaining.plan)  # ['inc', 'inc', 'inc', 'inc']\n</code></pre>"},{"location":"user_guide/getting_started/#goal-context-for-dynamic-actions","title":"Goal Context for Dynamic Actions","text":"<pre><code>class AdaptiveProvider:\n    def provide_actions(self, state, goal=None):\n        if goal and \"surgery\" in str(goal.target_state):\n            return [emergency_surgery_action]\n        return [treat_patient_action]\n\nplanner = Planner(providers=[AdaptiveProvider()])\nresult = planner.generate_plan(state, Goal(target_state={\"surgery\": True}))\n# Will include emergency_surgery_action\n</code></pre>"},{"location":"user_guide/getting_started/#6-execution-advanced","title":"6. Execution (Advanced)","text":"<p>In a real game loop, you would execute the first action in the plan, then re-evaluate the state or re-plan.</p> <p>See <code>examples/example1.py</code> for a complete runnable bot.</p>"}]}